#!/bin/bash

# Docker image name
IMAGE_NAME="nas"

# Container inner port (define more ports by separate them with spaces)
INNER_PORTS="22 445"

# Container name
CONTAINER_NAME="nas"

# Container port (define more ports by separate them with spaces)
CONTAINER_PORTS="22 445"

# default vars
NAME=nas
USER=user
PASS=pass
